# Alpha Round 2 Plan

**Goal:** implement the round-2 feedback on onboarding, and finish the four setup steps round 1
deferred.

**Source of truth:** `docs/feedback/round-2/onboarding-owner-feedback.md`, the owner's words,
verbatim, 2,473 words over 21 step sections. The ledger keyed to it is
`docs/feedback/round-2/ledger.md`, produced by brief H.

Round 1 and its 127 claims closed on 2026-09-06; see `docs/HANDOFF-2026-09-06.md`.

---

## Part A. Decisions taken, 2026-09-06

<!-- decision: sex-nd-requires-body-fat | status: adopted | supersedes: none -->
### A1. `ND` is allowed for sex, and it makes body fat REQUIRED

The owner asked (r2.11) that the sex control be deselectable, defaulting to `ND` for
non-disclosed, so someone uncomfortable answering can pass. **Ruled: yes, and body fat becomes
required on that path.**

The reason it cannot simply be optional is that sex is read by three engines, and one of them
cannot run without it:

| Engine | With a sex | With `ND` |
| --- | --- | --- |
| Resting metabolic rate, Mifflin-St Jeor | `+5` male, `-161` female | **cannot compute** |
| Resting metabolic rate, Cunningham | not used unless body fat is given | **works: no sex term** |
| Beverage target, IOM 2005 | 3000 mL male, 2200 mL female | no sex-free figure exists |
| Body fat from tape, US Navy | different equation per sex | **cannot compute** |

Averaging the two Mifflin-St Jeor constants would invent a coefficient, which this project does
not do. So `ND` is workable only through the equation that has no sex term, and that equation
needs fat-free mass, which needs a body-fat percentage.

**This is the route the app already recommends.** `src/content/sexRationale.ts` tells the reader
in as many words: supply a body-fat percentage and the app switches to the equation with no sex
term. The rule makes the screen behave the way the explainer already promises.

**Three consequences, all of which must ship together or the state is incoherent:**

1. `ND` makes the body-fat field **required**, on the same step. The `Not measured` option is
   unavailable while sex is `ND`.
2. **`ND` disables the tape method.** The Navy equations are structurally different per sex, not
   differently weighted, so the tape route cannot produce a number without one. With `ND`, body
   fat comes from the percentage entry only, and the tape option says why it is unavailable.
3. **The beverage target shows a RANGE.** Ruled by the owner: with `ND` the screen shows 2200 to
   3000 mL and states that the reference intake is published per sex. It does not pick one
   silently and it does not average them.

<!-- decision: wizard-commit-on-next | status: adopted | supersedes: none -->
### A2. A step's answers commit on Next, not on keystroke

> "the data of a field cell should not be replaced at the entry point (store it as temp), but it
> should be replaced only when 'NEXT' is pressed."

Adopted. It is what makes `ND` implementable as a default that Next overrides, and it makes Back
non-destructive.

**One tension to solve rather than discover.** Round 1's most valued feature was that setup
survives a closed browser (`AppState.setupDraft`, claim C1.G.1). If a field only reaches the
draft on Next, closing the browser mid-step loses everything typed on that step, which is exactly
the loss that feature exists to prevent.

**Resolution: persist BOTH tiers.** `setupDraft` keeps the committed answers; a second field
keeps the uncommitted buffer for the step in progress. Restoring returns the user to their step
with their half-typed values intact, and the committed answers stay untouched until they press
Next. Do not implement A2 without this, or a fix for one claim silently breaks another.

### A3. Assets are the owner's, and intake is not the peer's

The peer session is scoped to asset generation only. It briefly took the round-2 intake and the
owner corrected that; the intake subagent it had already dispatched is being allowed to finish
rather than killed mid-write, and the ledger belongs to this session from the moment it lands.

He has taken the asset work himself and closed that session. `docs/design/2026-09-06-asset-manifest.md`
and `-asset-prompts.md` are his. Placeholder frames stay in the UI until artwork arrives, and no
agent should draw, generate or re-specify an asset.

Two rows there are his to resolve, recorded so they are not lost: the r2.12 subsection sprite,
which cannot ship as the real person's likeness that was asked for and needs an original figure;
and the r2.19 reset icon, which he is supplying.

### A4. One entry per UTC offset is refused (r2.09)

He asked whether a conventional one-per-offset list exists. It does not, and it should not.
Measured against the platform's own 418 zones: 37 distinct offsets in January, and **16 of them
split into different offsets by July**. New York and Panama are both UTC-05:00 in January; in July
New York is -04:00 and Panama is not. Collapsing them breaks reminders for eight months of the
year, which is the defect `src/domain/export/fixtures/athens-dst-week.ics` exists to catch.

**What ships instead:** the list sorted by current offset then by name, which is the real defect
he reported, and `(UTC/GMT-05:00)` in the parentheses, which he asked for and which is reasonable.
Optionally, filtering aliases to canonical zones, which is an honest reduction; measure how many
it removes before promising it.

---

## Part A2. Rulings on the intake assessment, 2026-09-06

The ledger is `docs/feedback/round-2/ledger.md`, 91 rows. `assessment.md` raised fifteen rows as
needing a ruling. **Fourteen are ruled here; none needed the owner.** Round 1's lesson was that
parking work behind an invented blocker is the same failure as parking it behind a plan that does
not exist.

**Four reversals of round-1 decisions, all of them his and all of them allowed.**

| Row | Round 1 said | Round 2 says | Ruling |
| --- | --- | --- | --- |
| D2.12.2 | modal close upper LEFT (C1.07.11) | upper RIGHT, "more intuitive" | Round 2 wins. `FormCuesModal` already closes upper-right, so this makes the app consistent. The comment in `setup.css` recording the old ruling must be updated, not left contradicting the code |
| D2.13.3 / D2.13.4 | explanatory text BELOW the control | `Optional` above the percentage field, the estimate link below it | Not a contradiction. Round 1 was about the mode radio group; round 2 is about the number field. Follow round 2, which is more specific |
| D2.01.4 vs D2.04.1 | — | "remove the SKIP" and "works perfectly" | Not a conflict. r2.04 asked him to press Skip and confirm the intro does not return; the praise is for the seen-flag, which stays. The control still goes |
| D2.11.7 | draft persists on every keystroke | commit on Next | Both, per A2. Persist the committed draft AND the in-progress buffer |

**Three things the assessment found ALREADY BUILT.** Briefs corrected so no agent rebuilds them:

- **D2.14.1**, sex-conditioned tape fields. `SetupWizard.tsx:1815-1845` already renders hip for
  female only and already swaps the abdomen label. Only the `ND` branch is new.
- **D2.16.2**, the page shake. Already ships, already gated on `prefers-reduced-motion`, and
  `vibrate()` is already a no-op on desktop. The real work in r2.16 is the OTHER half: marking
  every missing field at once instead of one per press.
- **D2.15.5**, the descriptive line above each citation. `BodyEquation.computes` already is that
  sentence. The ask is consistency and styling.

**Two gates a naive build would trip.**

- **D2.14.3.** `disclosure.why` is one shared key with six call sites. Renaming it to `Disclaimer`
  would relabel every `why?` in the app. A NEW key, used once.
- **D2.12.6.** He asks for "a small emoji" and then calls it a "sprite". As a literal character it
  fails R6, which treats U+2600 to U+27BF as emoji. As an image asset it is not copy and R6 does
  not apply. Ruled: **image asset**, which is also what he means by sprite and what he is supplying.

**One row genuinely open, and it is his.** D2.09.4, whether to reduce the time-zone list. A2's A4
already refuses one-entry-per-offset with the measurement behind it. Filtering aliases to canonical
zones is an honest reduction; the number it saves must be measured before it is offered.

---

## Part B. The brief queue

Each is a file under `docs/plans/subagent-briefs/`. Dispatch order is set by file collisions, not
by importance: everything below touches `src/content/copy.ts` and most touch
`src/ui/setup/SetupWizard.tsx`. **Use `isolation: "worktree"` to run more than one at a time**,
and whichever branch merges second regenerates the catalogue.

State verified against `git log` on 2026-09-06 at `739b88e`. "Written" means the brief exists in
`docs/plans/subagent-briefs/`; it does not mean the code is written. Waves are the dispatch order in
`docs/plans/2026-09-06-ORCHESTRATION.md`. Re-derive this column rather than trusting it.

| Brief | Covers | Claims | State |
| --- | --- | --- | --- |
| F | Step 4: three sliders, five equipment tiers, per-tier questions, microplates removed | 13 | **SHIPPED**, `05e28b4` |
| G | The activity slider widened to nine stops, and its provenance callout | 1 | Written. Not dispatched. Wave 4 |
| I | Steps 5 to 7: the two-axis goal, target body fat, the feasibility calendar, the JIT split | 17 | Written. Not dispatched. Wave 4 |
| H | Round-2 intake: the ledger and the assessment | — | **DONE**. `docs/feedback/round-2/ledger.md` and `assessment.md` |
| **J** | The intro: remove Skip, ELI5 the prose, fade transitions, the tl;dr acknowledgement modal | r2.01 to r2.03 | Written, `af44819`. Not dispatched. Wave 2 |
| **K** | The body page: width, `ND` and commit-on-Next, the sex modal, the body-fat box, the tape fields, the references, the invalid-field cue | r2.10 to r2.16 | **MERGED** `3f43e11`. Wave 1, all seven claims |
| **L** | The guidance screen, rebuilt: ELI5, collapsible topics, four new sourced sections | r2.18 | Written, `af44819`. Not dispatched. Wave 3, one Opus agent |
| **M** | The review page: the acknowledgement tick, and the data-transfer explainer | r2.19 | Written, `af44819`. Not dispatched. Wave 5, last |
| **N** | The dynamic banner, the time-zone fixes, reaching Settings during setup | r2.05, r2.09, general | Written, `af44819`. Not dispatched. Wave 2 |
| — | The 139 Title Case corrections, R14 | — | Not started. Wave 6, needs a clear tree |

### Notes that belong on the briefs when they are written

**J.** Remove the Skip control entirely: he does not want the caveats skippable on a first run,
and the seen-flag already stops it returning. The tl;dr becomes a modal OVER the slide with a
checkbox carrying his exact sentence, corrected for grammar only. Do not make the language more
corporate; he said so explicitly and gave the reason.

**K is one brief, not several**, because r2.11 and r2.16 interact and he said to keep them
together. It carries A1 and A2 in full. It is also the brief that fixes the phone-width overflow,
which is a defect rather than a preference.

**L takes a single Opus agent**, on his instruction, not a fan-out of Sonnets. New sections:
fluid during a session with a worked example, cool-down and why fast repetitions do not build
muscle, electrolytes and when sports drinks are unnecessary, footwear and foot care. Every claim
needs a verified source or an explicit note that none was found; he will supply his own
recommendation where the evidence does not exist. **Never reach for a weak source to fill a gap.**

**M and N are small** and can share an agent if their file sets allow.

### Prose, across all of them

> "You are being too corporate and neutral."

That is a fair reading and it applies to every brief here. The house register for user-facing prose
in this app is: short sentences, bullet points over walls of text, a subsection heading before any
block longer than three lines, and emphasis used sparingly to break monotony. The `wait-what`
skill is the one he means by "I don't get it" and "ELI5"; load it before rewriting any explanatory
text. R14 still governs headings: Title Case, and a noun phrase.

---

## Part C. Carried over from round 1, still open

- **139 Title Case corrections**, R14, `scripts/check-title-case.mjs`. The script joins CI once
  they pass.
- **The body-fat chart shows one percentage range to both sexes.**
  `BODY_FAT_CHART_PERCENTAGES` is a single array; the icon register correctly specifies male 10 to
  35 and female 15 to 40. Split it. Blocked only by brief F holding the consumer file.
- **Body fat cannot be re-measured after setup.** `baselineBodyFatPct` has one writer and
  `BodyMassQuickLog` hard-codes null.
- **The installed app icon is the previous app's logo.** Now the owner's, under A3.
- **The Graphify graph is stale** by fourteen decisions.


---

## Part D. State at the close of the session that opened round 2

**Shipped and deployed.** Brief F: step 4 is three sliders and five equipment tiers, with the
exercise library untouched. Verified independently, 123 files and 2430 tests.

**Written, ruled, and waiting for a dispatcher.** Briefs G, I, J, K, L, M and N. The wave order and
the dispatch prompt are in `docs/plans/2026-09-06-ORCHESTRATION.md`. Do not invent a new order:
K changes the `Sex` type and the draft model, so it goes first and alone, and M goes last because
the review screen restates the other steps.

**Every round-2 ruling is taken.** Part A and Part A2 above. Fourteen of the fifteen rows the
assessment raised are ruled; the last, the time-zone reduction, is ruled in brief N at 59 zones.
None of them needs the owner again.

**What has NOT been done, in the order it is worth doing.**

1. The seven briefs. That is the round.
2. The 139 Title Case corrections, R14. All in `copy.ts`, so they need a clear tree.
3. `BODY_FAT_CHART_PERCENTAGES` is one array for both sexes; the register correctly says male 10
   to 35 and female 15 to 40. Essential body fat is 2 to 5 per cent in men and 10 to 13 in women,
   so the 10 per cent stop offered to a woman is below essential fat. Split it.
4. Body fat cannot be re-measured after setup: `baselineBodyFatPct` has one writer and
   `BodyMassQuickLog` hard-codes null.
5. The Graphify graph is stale by roughly twenty decisions.
6. Regenerate and republish the round-2 walkthrough page once the briefs land, to the SAME artifact
   URL so his links keep working.

**Owned by the owner, not by any agent.** All assets. He has the manifest, the prompts and the
register, and he is generating. Two rows are his to resolve: the subsection sprite, which cannot
be the real person's likeness that was asked for, and the reset icon, which he is supplying. The
installed app icon is still the previous app's `W.CONSOLE` logo and still ships.
