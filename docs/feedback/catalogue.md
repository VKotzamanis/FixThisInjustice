# Alpha review catalogue

Generated by `scripts/alpha-catalogue.mjs` from the tree. Nothing here is retyped.
Regenerate after any change to a copy table or a component:

```bash
node scripts/alpha-catalogue.mjs
```

Built from `8536108`. 99 parts across 13 screens, holding 477 of the table's 480 copy keys; 3 have no surface and are excused by name at the foot.

## boot (2)

### `boot.sequence`

Boot sequence. The dotted step lines that print on the first open.

Renders: `src/ui/components/Boot.tsx`

States: `running`, `finished`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `hero.boot` | Starting up | warming up | same | yes |
| `status.bootConsole` | FTI CONSOLE v3 | limelight console v3 | same | yes |
| `status.bootOk` | OK | yes | same | yes |
| `status.bootPlan` | Loading plan | cueing the plan | same | yes |
| `status.bootPlanName` | plan {name} | the {name} era | same | yes |
| `status.bootReady` | READY. | she's ready. | same | yes |
| `status.bootSchedule` | {count} sessions per week | {count} shows per week | same | yes |
| `status.bootStore` | Restoring local store | unpacking the archive | same | yes |
| `status.bootWeek` | week {week} of {weeks} | same | same | yes |

### `boot.skip`

Skip control. The control that ends the sequence, and the any-key skip.

Renders: `src/ui/components/Boot.tsx`

States: `visible`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.skipBoot` | Skip | skip it | same | yes |

## shell (7)

### `shell.topbar`

Top bar. The brand line and the loading or loaded status word.

Renders: `src/app/App.tsx`

States: `loading`, `loaded`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `shell.status.loaded` | local data loaded | same | same | yes |
| `shell.status.loading` | reading local data | same | same | yes |

### `shell.session-indicator`

Session indicator. The plan position shown in the header from every view.

Renders: `src/ui/components/SessionIndicator.tsx`

States: `no-plan`, `idle`, `in-progress`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `hero.programmeComplete` | Programme complete. | the whole run. done. | same | yes |
| `label.planPosition` | Plan position | same | same | yes |
| `status.planComplete` | complete | wrapped | same | yes |
| `status.sessionCursor` | Session {shown} of {total} | ep. {shown} of {total} | SERVICE {shown} OF {total} | yes |

### `shell.tab-strip`

Tab strip. The seven tabs, in the order src/ui/nav/views.ts sets.

Renders: `src/app/App.tsx`, `src/ui/nav/views.ts`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `nav.atlas` | Atlas | the collection | CATALOGUE | yes |
| `nav.label` | Views | the views | same | yes |
| `nav.log` | Log | the archive | ARCHIVE | yes |
| `nav.plan` | Plan | the run | SCHEDULE | yes |
| `nav.settings` | Settings | backstage | CONTROLS | yes |
| `nav.targets` | Targets | the numbers | same | yes |
| `nav.today` | Today | today | TODAY | yes |
| `nav.train` | Train | showtime | DEPARTURES | yes |

### `shell.spotlight-button`

Spotlight button. The control that opens the command palette.

Renders: `src/ui/components/SpotlightButton.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.openSpotlight` | Search | find it | same | yes |

### `shell.update-prompt`

Update prompt. The banner offering a reload when a new build is waiting.

Renders: `src/app/UpdatePrompt.tsx`

States: `hidden`, `offered`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `banner.update.body` | A new version is ready. | same | same | yes |
| `banner.update.tag` | UPDATE READY | same | same | yes |
| `button.reload` | Reload | same | same | yes |

### `shell.save-error`

Save error banner. The banner shown when the document could not be written.

Renders: `src/app/App.tsx`

States: `hidden`, `raised`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `banner.saveFailed.body` | The change could not be saved. The stored copy is unchanged. | same | same | yes |
| `banner.saveFailed.tag` | SAVE FAILED | same | same | yes |
| `banner.saveQuota.body` | Storage is full. Export now. | same | same | yes |
| `banner.saveQuota.tag` | STORAGE FULL | same | same | yes |
| `banner.saveUnavailable.body` | Storage is unavailable here. Export now. | same | same | yes |
| `banner.saveUnavailable.tag` | NO STORAGE | same | same | yes |
| `button.exportData` | Export data | same | same | yes |
| `button.exportStoredCopy` | Export stored copy | same | same | yes |
| `button.retrySave` | Retry save | try again | same | yes |

### `shell.load-error`

Load error banner. The banner shown when the stored document could not be read.

Renders: `src/app/App.tsx`

States: `hidden`, `raised`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `banner.loadInvalid.body` | Stored data did not validate: {reason}. Nothing was overwritten. | same | same | yes |
| `banner.loadInvalid.tag` | INVALID DATA | same | same | yes |
| `button.exportStoredData` | Export stored data | same | same | yes |

## setup (11)

### `setup.progress`

Step counter. The "step N of 9" line and the step title above every screen.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `group.goal` | Fitness Goal & Schedule | same | same | yes |
| `group.personal` | Personal Information | same | same | yes |
| `group.settings` | Preferred Settings | same | same | yes |
| `setup.hero` | Setup | same | same | yes |

### `setup.units`

Step 1, Units. Metric or imperial, and what the choice does to stored values.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.unitsOnce` | Makes logging stuff easier. Change at any point in Settings. | same | same | yes |
| `label.units` | Units on the weight plates | same | same | yes |
| `label.unitsImperial` | Pounds (lb) | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `label.unitsMetric` | Kilograms (kg) | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `step.units` | Units | same | same | yes |

### `setup.timezone`

Step 2, Time zone. The zone every civil date in the app is computed in.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.timezoneDetected` | Necessary for the notification bot and the week planner. | same | same | yes |
| `advice.timezoneInvalid` | Not a recognised IANA time zone. | same | same | yes |
| `advice.timezonePick` | Select from the drop down menu: | same | same | yes |
| `label.timezone` | Time zone | same | same | yes |
| `step.timezone` | Time zone | same | same | yes |

### `setup.body`

Step 3, Body. Body mass, stature, age, sex, the methods list, and the optional tape estimate.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`, `blocked`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.bodyFatEstimate` | 21.9 % body fat, ± 3.52 percentage points. | same | same | yes |
| `advice.bodyFatOptional` | Optional. With it, RMR uses the Cunningham equation. | same | same | yes |
| `advice.tapeMethod` | US Navy circumference method. Keep the tape level and snug. | same | same | yes |
| `advice.tapeNeedFemale` | Enter neck, abdomen I and hip girths. | same | same | yes |
| `advice.tapeNeedMale` | Enter neck and abdomen II girths. | same | same | yes |
| `advice.tapeOutOfDomain` | Girths outside the equation's domain. No estimate is shown. | same | same | yes |
| `disclosure.why` | why? | same | same | no: CASE ALONE, OR THE LIMELIGHT WORD ALREADY |
| `error.positive` | Enter a number above zero. | same | same | no: THE NUMBER-ENTRY ERRORS |
| `error.valueRequired` | Enter a number. | same | same | no: THE NUMBER-ENTRY ERRORS |
| `error.wholeNumber` | Enter a whole number. | same | same | yes |
| `label.bodyFatKnown` | Known percentage | same | same | yes |
| `label.bodyFatNone` | Not measured | same | same | yes |
| `label.bodyFatTape` | Estimate from tape measurements | same | same | yes |
| `label.centimetres` | Centimetres | same | same | yes |
| `label.feet` | Feet | same | same | yes |
| `label.inches` | Inches | same | same | yes |
| `label.metres` | Metres | same | same | yes |
| `label.name` | How should I refer to you? | same | same | yes |
| `label.sex` | Biological sex | same | same | yes |
| `label.sexFemale` | Female | same | same | yes |
| `label.sexMale` | Male | same | same | yes |
| `quantity.abdomenI` | Abdomen I | same | same | yes |
| `quantity.abdomenII` | Abdomen II | same | same | yes |
| `quantity.age` | Age | same | same | yes |
| `quantity.bodyFat` | Body fat | same | same | yes |
| `quantity.bodyMass` | Body mass | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `quantity.height` | Height | same | same | yes |
| `quantity.hip` | Hip | same | same | yes |
| `quantity.neck` | Neck | same | same | yes |
| `step.body` | Body | same | same | yes |
| `why.bodyFatEstimate` | The figure after ± is the standard error of the US Navy estimate against hydrostatic weighing. It is larger than most changes worth chasing, so track change over time rather than the absolute number. | same | same | yes |
| `why.bodyFatOptional` | With a body-fat value: Cunningham for RMR, protein per kg of fat-free mass. Without one: Mifflin-St Jeor, protein per kg of body mass. | same | same | yes |

### `setup.training`

Step 4, Training context. Experience and activity, which drive the energy target.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.loadSteps` | The smallest increment a suggested load uses. | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `label.activity` | Activity level | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `label.equipment` | Equipment | the gear | same | yes |
| `label.experience` | Experience | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `label.microPlates` | Micro-plates available | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `option.activityModerate` | Moderate: regular walking, active job or training | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.activitySedentary` | Sedentary: desk work, little walking | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.activityVigorous` | Vigorous: heavy physical work, daily hard training | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.equipmentBodyweight` | Bodyweight only | same | same | yes |
| `option.equipmentDumbbells` | Dumbbells only | same | same | yes |
| `option.equipmentFullGym` | Full gym | same | same | yes |
| `option.experienceAdvanced` | Advanced: over three years | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.experienceIntermediate` | Intermediate: one to three years | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.experienceNovice` | Novice: under one year of consistent training | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `quantity.barbellStep` | Barbell step | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `quantity.dumbbellStep` | Dumbbell step, per pair | same | same | yes |
| `quantity.microPlateStep` | Micro-plate step | same | same | yes |
| `quantity.stackStep` | Weight-stack step | same | same | yes |
| `step.training` | Training context | same | same | yes |

### `setup.goal`

Step 5, Goal. Fat loss, maintenance, muscle gain or recomposition.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.creatineOnly` | Dose scales with body mass. | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `advice.weighIn` | Optional. Compares body mass before and after. | same | same | yes |
| `label.creatine` | Creatine monohydrate | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `label.goal` | Goal | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `label.targetDate` | Target date (optional) | same | same | yes |
| `label.weighIn` | Weigh in | same | same | yes |
| `option.goalFatLoss` | Fat loss | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.goalMaintenance` | Maintenance | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.goalMuscleGain` | Muscle gain | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `option.goalRecomposition` | Recomposition | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `quantity.targetBodyMass` | Target body mass | same | same | yes |
| `step.goal` | Goal | same | same | yes |

### `setup.availability`

Step 6, Availability. The days and the equipment the plan may use.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `error.pickOneDay` | Select at least one weekday. | same | same | yes |
| `label.duration` | duration | same | same | yes |
| `label.sessionsPerWeek` | Sessions per week | same | same | yes |
| `label.startTime` | start time | same | same | yes |
| `quantity.weeklySessionTarget` | Weekly session target | same | same | yes |
| `step.availability` | Availability | same | same | yes |
| `weekday.friday` | Friday | same | same | yes |
| `weekday.monday` | Monday | same | same | yes |
| `weekday.saturday` | Saturday | same | same | yes |
| `weekday.sunday` | Sunday | same | same | yes |
| `weekday.thursday` | Thursday | same | same | yes |
| `weekday.tuesday` | Tuesday | same | same | yes |
| `weekday.wednesday` | Wednesday | same | same | yes |

### `setup.programme`

Step 7, Programme length. How many weeks the generated plan runs for.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.deloadEveryFourth` | Every fourth week halves set counts, load unchanged. | same | same | yes |
| `label.includeCardio` | One conditioning block per week | same | same | yes |
| `quantity.programmeWeeks` | Programme length | same | same | yes |
| `step.programme` | Programme length | same | same | yes |

### `setup.guidance`

Step 8, Guidance. Supplement and kit guidance, inside the wizard.

Renders: `src/ui/setup/SetupWizard.tsx`, `src/ui/setup/GuidanceScreen.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `hero.guidance` | A few things worth knowing | same | same | yes |
| `step.guidance` | Getting started | same | same | yes |

### `setup.review`

Step 9, Review. The summary, and the control that writes the profile.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `ready`, `blocked`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.confirmStart` | Confirm and start | lock it in | same | yes |
| `hero.dailyTargets` | Daily targets | the numbers | same | yes |
| `hero.programme` | Programme | same | same | yes |
| `label.creatineDose` | Creatine | same | same | yes |
| `label.energy` | Energy | same | same | yes |
| `label.expectedRate` | Expected rate | same | same | yes |
| `label.fluid` | Fluid (beverages) | same | same | yes |
| `label.inTargetRange` | In the target set range | same | same | yes |
| `label.maintenanceOnly` | Maintenance only | same | same | yes |
| `label.none` | none | same | same | yes |
| `label.protein` | Protein | same | same | yes |
| `status.rateUnknown` | Not established by the evidence base | same | same | yes |
| `status.targetsNotEstimated` | Targets are not estimated for these entries. Check the body screen. | same | same | yes |
| `step.review` | Review | same | same | yes |

### `setup.nav`

Wizard navigation. Back and Continue, and what a blocked step does to them.

Renders: `src/ui/setup/SetupWizard.tsx`

States: `default`, `blocked`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.back` | Previous | same | same | yes |
| `button.continue` | Next | same | PROCEED | yes |

## today (13)

### `today.marquee`

Marquee. The scrolling ticker strip at the top of Today.

Renders: `src/ui/components/Marquee.tsx`, `src/ui/format/weekDelta.ts`

States: `scrolling`, `reduced-motion`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.pauseTicker` | Pause ticker | pause the ticker | PAUSE TICKER | yes |
| `status.weekDeltaNegative` | Weekly target missed. {completed} of {target} sessions completed. | {completed} of {target}. flop era. | {completed} OF {target} DEPARTED. SERVICE DISRUPTED. | yes |
| `status.weekDeltaPositive` | Weekly target exceeded. {completed} of {target} sessions completed. | {completed} of {target}. no crumbs. | {completed} OF {target} DEPARTED. EXTRA SERVICE. | yes |
| `status.weekDeltaZero` | Weekly target met. {completed} of {target} sessions completed. | {completed} of {target}. she delivered. | {completed} OF {target} DEPARTED. ALL ON TIME. | yes |

### `today.refusal-banner`

Refusal banner. The line that reports why a control was refused, and its dismiss.

Renders: `src/ui/views/TodayView.tsx`, `src/ui/format/refusal.ts`

States: `hidden`, `raised`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `banner.actionRefused.tag` | NOT APPLIED | same | same | no: CASE ALONE, OR THE LIMELIGHT WORD ALREADY |
| `button.dismiss` | Dismiss | noted. | same | yes |
| `status.refusalAlreadyStarted` | This session already started on {date}. | same | same | yes |
| `status.refusalLabelNotOffered` | {label} is not offered on {date}. | same | same | yes |
| `status.refusalNotNextDay` | {date} is not the next session day. | same | same | yes |
| `status.refusalPaused` | The plan is paused on {date}. | the plan is on hiatus on {date}. | same | yes |
| `status.refusalSessionOpen` | A session is already in progress on {date}. | same | same | yes |
| `status.refusalUnrecognised` | That change was refused. | same | same | yes |

### `today.hero`

Hero line. The one line Today is about, and the eyebrow above it.

Renders: `src/ui/views/TodayView.tsx`

States: `no-plan`, `scheduled`, `in-progress`, `paused`, `skipped`, `completed`, `rest-day`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.completeSetup` | Complete setup to generate a plan. | no setup, no plan. sort it out. | same | yes |
| `advice.nextSession` | Next: {weekday} {startTime} {label}. | same | same | yes |
| `advice.noSessionIn14Days` | No sessions in the next 14 days. | nothing booked for 14 days. dry spell. | same | yes |
| `hero.noPlan` | No plan. | no plan. tragic. | same | yes |
| `hero.noSessionToday` | No session today. | day off, apparently. | same | yes |
| `hero.sessionCompleted` | Session completed. | ate. | DEPARTED | yes |
| `hero.sessionInProgress` | Session in progress. | the show is running. | same | yes |
| `hero.sessionSkipped` | Session skipped. | skipped. next. | same | yes |

### `today.session-card`

Session card. The exercise list for today, with sets, reps and rest.

Renders: `src/ui/views/TodayView.tsx`

States: `scheduled`, `rest-day`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `status.setsBy` | {sets} × {prescription} | same | same | yes |

### `today.start`

Start control. The control that opens the session.

Renders: `src/ui/views/TodayView.tsx`

States: `enabled`, `in-progress`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.returnToSession` | Return to session | back on stage | same | yes |
| `button.startSession` | Start session | LET'S GO BABES | BOARD | yes |

### `today.mark-completed`

Mark completed control. The control that closes today session without opening it.

Renders: `src/ui/views/TodayView.tsx`

States: `offered`, `hidden`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.markCompleted` | Mark completed | mark it done | MARK DEPARTED | yes |

### `today.label-picker`

Label picker. The control that trades today session for another label the week still owes.

Renders: `src/ui/views/TodayView.tsx`

States: `closed`, `open`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.trainLabelToday` | Train {label} today | same | same | yes |
| `button.trainSomethingElse` | Train something else | plot twist | REBOOK | yes |
| `status.remainingThisWeek` | Remaining this week | still to come this week | same | yes |

### `today.week-stamp`

Week stamp. The stamp a week at or above its target earns.

Renders: `src/ui/components/WeekStamp.tsx`

States: `hidden`, `stamped`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `status.weekMetStamp` | Target met | MOTHER | ALL DEPARTED | yes |

### `today.intervention`

Intervention. The missed-week panel on Today, not the popup.

Renders: `src/ui/components/Intervention.tsx`

States: `hidden`, `raised`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.interventionBody` | The week is over. The next session stands. | the week flopped, not you. the next slot still stands. | same | yes |
| `hero.weeklyTargetMissed` | Weekly target missed | the intervention | IRREGULAR OPERATIONS | yes |

### `today.time-capsule`

Time capsule. The capsule entry point on Today.

Renders: `src/ui/components/TimeCapsule.tsx`

States: `none`, `sealed`, `openable`, `opened`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.capsuleOpenDatePassed` | The open date has passed. | same | same | yes |
| `advice.timeCapsule` | A note to your future self, sealed until a date. | a note to future you, sealed until a date you pick. | same | yes |
| `button.openCapsule` | Open capsule | open it | same | yes |
| `button.readCapsule` | Read capsule | read it | same | yes |
| `button.writeCapsule` | Write capsule | write it | same | yes |
| `hero.timeCapsule` | Time capsule | a note for later | same | yes |
| `status.capsuleSealed` | Opens {date}, in {days}. | same | same | yes |
| `status.capsuleWritten` | Sealed {date}. | same | same | yes |

### `today.strip`

Fourteen-day strip. The fourteen day glyphs and their accessible names.

Renders: `src/ui/views/TodayView.tsx`

States: `planned`, `live`, `served`, `no-show`, `on-hiatus`, `off`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `hero.nextFourteenDays` | Next 14 days | the next 14 dates | same | yes |
| `status.dayCompleted` | Completed | served | DEPARTED | yes |
| `status.dayInProgress` | In progress | live | BOARDING | yes |
| `status.dayPaused` | Paused | on hiatus | HELD | yes |
| `status.dayPlanned` | Planned | booked | SCHEDULED | yes |
| `status.dayRest` | Rest | day off | NO SERVICE | yes |
| `status.daySkipped` | Skipped | no show | CANCELLED | yes |

### `today.pause`

Pause control. The control that stops the plan, and the paused line.

Renders: `src/ui/views/TodayView.tsx`

States: `pausable`, `paused`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.pauseHoldsCursor` | Paused days consume no session. | a hiatus costs you nothing. | same | yes |
| `button.pausePlan` | Pause plan | on hiatus | HOLD SCHEDULE | yes |
| `button.resumePlan` | Resume plan | comeback tour | same | yes |
| `status.planPaused` | Plan paused since {date}. | same | same | yes |

### `today.skip`

Skip control. The skip control and its optional reason field.

Renders: `src/ui/views/TodayView.tsx`

States: `closed`, `open`, `skipped`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.cancel` | Cancel | never mind | CANCEL | yes |
| `button.confirmSkip` | Confirm skip | yes skip it | same | yes |
| `button.skipToday` | Skip today | not today satan | CANCEL SERVICE | yes |
| `label.skipReason` | Reason (optional) | the story (optional) | same | yes |
| `status.skipReason` | Reason: {reason} | same | same | yes |

## train (8)

### `train.header`

Session header. The session name, the eyebrow and the deload note.

Renders: `src/ui/views/TrainView.tsx`

States: `no-profile`, `no-session`, `in-progress`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.deloadBlock` | Deload block: set count reduced, load held. | deload block: fewer sets, same load. | same | yes |
| `advice.noProfileTrain` | No profile. Complete setup first. | no profile. setup first, babes. | same | yes |
| `advice.noSessionToday` | No session assigned today. Pick one on Today. | nothing assigned today. pick one on today. | NO SERVICE TODAY. PICK ONE ON TODAY. | yes |
| `hero.train` | TRAIN | showtime | DEPARTURES | yes |
| `status.sessionEyebrow` | SESSION {ordinal}, {label} | ep. {ordinal}, {label} | same | yes |

### `train.exercise-card`

Exercise card. One exercise, its prescription and its suggested load.

Renders: `src/ui/views/train/ExerciseCard.tsx`

States: `pending`, `active`, `done`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.addSet` | Add set | one more | same | yes |
| `button.formCues` | Form cues | same | same | no: THE TECHNIQUE REFERENCE |
| `button.formReference` | Form reference | same | same | no: THE TECHNIQUE REFERENCE |
| `label.noSuggestedLoad` | No load suggested | unscripted | same | yes |
| `label.suggestedLoad` | Suggested {load}: {kind} | same | same | yes |
| `status.adviceAddLoad` | Add load | same | same | no: THE PROGRESSION ADVICE KINDS |
| `status.adviceDeload` | Deload | same | same | no: THE PROGRESSION ADVICE KINDS |
| `status.adviceExtendReps` | Extend repetitions | same | same | no: THE PROGRESSION ADVICE KINDS |
| `status.adviceHold` | Hold load | same | same | no: THE PROGRESSION ADVICE KINDS |
| `status.lastSessionSets` | Last session {date}: {load} × {reps} | last show {date}: {load} × {reps} | same | yes |

### `train.set-row`

Set row. One set: load, reps, the log control and the delete.

Renders: `src/ui/views/train/SetRow.tsx`

States: `planned`, `logged`, `bonus`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.durationNeeded` | Enter the seconds held, above zero. | same | same | no: THE NUMBER-ENTRY ERRORS |
| `button.deleteSet` | Delete | same | same | no: RULE 4, a control whose misreading costs data |
| `button.logSet` | Log | log it | same | yes |
| `coach.setDeleted` | Set deleted. | set deleted. gone. | same | yes |
| `label.bonusSet` | BONUS | encore | same | yes |
| `status.setCounter` | SET {n}/{targetSets} | set {n} of {targetSets} | same | yes |

### `train.rest-panel`

Rest panel. The rest countdown, the extend control and the skip.

Renders: `src/ui/views/train/RestTimerPanel.tsx`

States: `hidden`, `running`, `elapsed`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.extendRest` | +30 s | +30 s | +30 s DELAY | yes |
| `button.skipRest` | Skip | i'm ready | EARLY DEPARTURE | yes |
| `notification.restOver` | Rest over | rest over. back on stage. | same | yes |
| `status.rest` | REST | catch ur breath | GATE HOLD | yes |
| `status.restRemaining` | {minutes}:{seconds} | same | same | yes |

### `train.hydration-banner`

Hydration banner. The beverage intake cue and its log control.

Renders: `src/ui/views/train/HydrationBanner.tsx`, `src/domain/training/hydration.ts`

States: `hidden`, `shortfall`, `met`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.beverageShortfall` | Beverage intake {logged} of {target} today. | beverage intake {logged} of {target} today. top it up. | same | yes |
| `advice.drinkToThirst` | Drink to thirst. | hydrate or diedrate | REFRESHMENT: DRINK TO THIRST | yes |
| `advice.logPostSessionMass` | Log post-session body mass. | same | same | yes |
| `button.logVolume` | Log {volume} | hydrate {volume} | same | yes |
| `why.postSessionMass` | A loss above 2 % of pre-session mass means fluid replacement was inadequate (ACSM 2007). | same | same | no: BODY MASS AND FLUID LOSS |

### `train.add-custom-exercise`

Add custom exercise. The form that adds an exercise the plan does not carry.

Renders: `src/ui/views/train/AddCustomExercise.tsx`

States: `closed`, `open`, `refused`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.addExercise` | Add exercise | same | same | no: THE CUSTOM-EXERCISE FORM |
| `button.saveExercise` | Add | same | same | no: THE CUSTOM-EXERCISE FORM |
| `label.modality.barbell` | Barbell | same | same | no: THE EQUIPMENT NAMES |
| `label.modality.bodyweight` | Bodyweight | same | same | no: THE EQUIPMENT NAMES |
| `label.modality.cable` | Cable | same | same | no: THE EQUIPMENT NAMES |
| `label.modality.dumbbell` | Dumbbell | same | same | no: THE EQUIPMENT NAMES |
| `label.modality.machine` | Machine | same | same | no: THE EQUIPMENT NAMES |
| `quantity.exerciseName` | Exercise name | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `status.customExerciseRefused` | The exercise was not added. | nope. the exercise was not added. | same | yes |
| `unit.characters` | characters | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |

### `train.body-mass-quick-log`

Body mass quick log. The pre- and post-session body mass entry.

Renders: `src/ui/views/train/BodyMassQuickLog.tsx`

States: `empty`, `logged`, `fluid-loss`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.fluidLoss` | Fluid loss above 2 %. Replace over the next hours. | same | same | no: BODY MASS AND FLUID LOSS |
| `button.logBodyMass` | Log body mass | same | same | no: BODY MASS AND FLUID LOSS |
| `quantity.postSessionBodyMass` | Post-session body mass | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `quantity.preSessionBodyMass` | Pre-session body mass | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `why.fluidLoss` | Loss of {loss} % of pre-session mass, above the {threshold} % threshold (ACSM 2007). | same | same | yes |

### `train.completion`

Session completion. The finish control and the line that follows it.

Renders: `src/ui/views/TrainView.tsx`

States: `incomplete`, `complete`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.backToToday` | Back to Today | same | same | no: CASE ALONE, OR THE LIMELIGHT WORD ALREADY |
| `button.finishSession` | Finish session | that's a wrap | CLOSE SERVICE | yes |

## plan (4)

### `plan.block-strip`

Block strip. The training and deload blocks, with their session ranges.

Renders: `src/ui/views/PlanView.tsx`

States: `training`, `deload`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.block` | Block {number} | same | same | yes |
| `label.blockStrip` | Training blocks | same | same | no: THE TWO TRAINING TERMS |
| `status.blockSessions` | sessions {from}–{to} | same | same | yes |
| `status.deloadNote` | volume −{cutPct} %, load unchanged | same | same | yes |
| `status.deloadTag` | DELOAD | same | same | no: THE TWO TRAINING TERMS |
| `why.deloadSets` | Each set count is the planned count times this block's set modifier of 0.5, rounded to the nearest whole set, minimum 1. The load is unchanged. | same | same | yes |

### `plan.week-scrubber`

Week scrubber. The control that moves the view a week at a time.

Renders: `src/ui/views/PlanView.tsx`

States: `first-week`, `middle`, `last-week`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.week` | Week | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `label.weekOfCount` | Week {shown} of {total} | same | same | yes |

### `plan.session-cards`

Session cards. One card per session, with its exercises and rest.

Renders: `src/ui/views/PlanView.tsx`

States: `past`, `next`, `future`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `status.nextSession` | next | same | same | no: CASE ALONE, OR THE LIMELIGHT WORD ALREADY |

### `plan.deep-link`

Deep link to a session. The control that opens a session from a plan card.

Renders: `src/ui/views/PlanView.tsx`

States: `enabled`, `no-plan`

No copy key. Review its layout and behaviour only.

## targets (8)

### `targets.energy`

Energy target. The daily energy figure and its unit.

Renders: `src/ui/views/TargetsView.tsx`

States: `default`

No copy key. Review its layout and behaviour only.

### `targets.protein`

Protein target. The daily protein figure and its unit.

Renders: `src/ui/views/TargetsView.tsx`

States: `default`

No copy key. Review its layout and behaviour only.

### `targets.fluid`

Fluid target. The daily beverage intake target.

Renders: `src/ui/views/TargetsView.tsx`

States: `default`

No copy key. Review its layout and behaviour only.

### `targets.expected-rate`

Expected rate. The expected rate of body mass change.

Renders: `src/ui/views/TargetsView.tsx`

States: `default`, `maintenance`

No copy key. Review its layout and behaviour only.

### `targets.creatine-dose`

Creatine dose. The daily dose and the sentence that defines it.

Renders: `src/ui/views/TargetsView.tsx`

States: `default`

No copy key. Review its layout and behaviour only.

### `targets.why`

The why disclosure. RMR and TDEE behind the "why?" summary, under contract R9.

Renders: `src/ui/views/TargetsView.tsx`

States: `closed`, `open`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.rmr` | Resting metabolic rate | same | same | yes |
| `label.tdee` | Total daily energy expenditure | same | same | yes |
| `why.beverageDefault` | IOM 2005 beverage share of the total-water adequate intake, DOI 10.17226/10925. The adequate intake for total water is 3.7 L per day for men and 2.7 L for women, of which beverages supply 3.0 L and 2.2 L. Water in food supplies the rest and is not counted here, because an app cannot measure it. | same | same | yes |

### `targets.intake-check-in`

Intake check-in. The daily energy and beverage entry, and its progress lines.

Renders: `src/ui/views/TargetsView.tsx`

States: `empty`, `partial`, `met`, `error`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.intakeRejected` | Daily total outside the accepted range. | same | same | yes |
| `button.recordIntake` | Record intake | same | same | yes |
| `hero.intakeCheckIn` | Intake check-in | same | same | yes |
| `label.energyProgress` | Energy against target | same | same | yes |
| `label.proteinProgress` | Protein against the lower bound | same | same | yes |
| `quantity.energyIntake` | Energy consumed today | same | same | yes |
| `quantity.proteinIntake` | Protein consumed today | same | same | yes |

### `targets.empty`

Targets empty state. What Targets says before a profile exists.

Renders: `src/ui/views/TargetsView.tsx`

States: `no-profile`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.noProfileSetupFirst` | No profile. Complete setup first. | no profile. setup first, babes. | same | yes |

## log (5)

### `log.body-mass-chart`

Body mass chart. The body mass series and its axes.

Renders: `src/ui/components/BodyMassChart.tsx`, `src/ui/views/LogView.tsx`

States: `empty`, `plotted`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.noBodyMassLogged` | No body mass logged. | same | same | yes |
| `hero.bodyMass` | Body mass | same | same | yes |

### `log.compliance-grid`

Compliance grid. The grid of sessions met and missed by week.

Renders: `src/ui/components/ComplianceGrid.tsx`

States: `empty`, `plotted`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.noWeeksYet` | No weeks to show. | same | same | yes |
| `hero.compliance` | Compliance | the attendance | same | yes |
| `label.complianceGrid` | Session compliance by week | same | same | yes |
| `status.complianceWeek` | Week of {monday}: {completed} of {target} completed | same | same | yes |
| `status.markCompleted` | Completed | same | same | yes |
| `status.markMissed` | Missed | same | same | yes |
| `status.markNotPlanned` | Not planned | same | same | yes |
| `status.markPlanned` | Planned | same | same | yes |
| `status.markSkipped` | Skipped | same | same | yes |

### `log.amrap-spark`

AMRAP spark. The reps-per-week spark line.

Renders: `src/ui/components/AmrapSpark.tsx`

States: `empty`, `plotted`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `hero.repsPerWeek` | Best reps per week | same | same | yes |
| `status.amrapBest` | {name}: best {reps} reps in one set | same | same | yes |

### `log.records`

Personal records. The record list and what counts as a record.

Renders: `src/ui/components/PRList.tsx`

States: `empty`, `listed`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.noSetsLogged` | No sets logged. | no sets yet. start something. | same | yes |
| `hero.personalRecords` | Personal records | the highlight reel | same | yes |
| `status.estimated1RM` | {load} estimated 1RM | same | same | yes |
| `status.noEstimated1RM` | No estimated 1RM | same | same | yes |

### `log.empty`

Log empty state. What Log says before anything has been logged.

Renders: `src/ui/views/LogView.tsx`

States: `no-profile`, `no-data`

No copy key. Review its layout and behaviour only.

## atlas (6)

### `atlas.count`

Collected count. The count line above the grid.

Renders: `src/ui/views/AtlasView.tsx`

States: `none`, `partial`, `complete`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.atlas` | Every logged set may add a card. | a field journal. every logged set might drop a card. | same | yes |
| `hero.atlas` | Atlas | same | same | yes |
| `label.atlasCollected` | Collected | same | same | yes |

### `atlas.rarity.common`

Common section. The common rarity heading, count and grid.

Renders: `src/ui/views/AtlasView.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.rarityCommon` | Common | same | same | yes |

### `atlas.rarity.uncommon`

Uncommon section. The uncommon rarity heading, count and grid.

Renders: `src/ui/views/AtlasView.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.rarityUncommon` | Uncommon | same | same | yes |

### `atlas.rarity.rare`

Rare section. The rare rarity heading, count and grid.

Renders: `src/ui/views/AtlasView.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.rarityRare` | Rare | same | same | yes |

### `atlas.locked-slot`

Locked slot. An undiscovered card, and the name a screen reader hears.

Renders: `src/ui/views/AtlasView.tsx`

States: `locked`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `status.undiscovered` | UNDISCOVERED | still a mystery | same | yes |

### `atlas.detail-dialog`

Card dialog. The card body, its citation and the close control.

Renders: `src/ui/views/AtlasView.tsx`

States: `open`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.closeModal` | Close | same | same | yes |
| `label.atlasSource` | Source | same | same | yes |

## settings (14)

### `settings.profile`

Profile fields. The active profile, the display unit and the editable profile values.

Renders: `src/ui/views/SettingsView.tsx`

States: `default`, `invalid`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.storedUnitsUnchanged` | Stored values never change. Mass is held in kg. | same | same | no: RULE 4, a control whose misreading costs data |
| `error.outsideAccepted` | Outside the accepted range. | same | same | no: THE NUMBER-ENTRY ERRORS |
| `hero.profile` | Profile | who you are | same | yes |
| `label.activeProfile` | Active profile | same | same | no: THE PROFILE FIELDS THE TARGETS ARE COMPUTED FROM |
| `label.displayUnit` | Display unit | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |

### `settings.equipment-steps`

Equipment steps. The load increments the suggested load is rounded to.

Renders: `src/ui/views/SettingsView.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `hero.equipmentSteps` | Equipment steps | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |

### `settings.hydration`

Hydration row. The daily beverage target and the default it starts from.

Renders: `src/ui/views/SettingsView.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.beverageDefault` | Default for the stated sex: 3000 mL per day. | same | same | yes |
| `hero.hydration` | Hydration | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |
| `quantity.dailyBeverageTarget` | Daily beverage target | same | same | no: THE QUANTITIES, THE UNITS AND THE INCREMENTS |

### `settings.reminders`

Reminders panel. The push state line and the enable control.

Renders: `src/ui/components/ReminderSettingsPanel.tsx`

States: `not-configured`, `unsupported`, `needs-install`, `denied`, `off`, `needs-reenable`, `active`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.reminderSubscribeFailed` | The browser refused to register this device. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `advice.reminderSyncFailed` | The schedule did not reach the server. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `hero.reminders` | Reminders | call times | same | yes |
| `label.remindersEnable` | Enable reminders | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `status.remindersActive` | Reminders are on. Schedule last sent at {time}. | same | same | yes |
| `status.remindersDenied` | Notifications are blocked. Allow them in browser site settings. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `status.remindersNeedInstall` | Add this app to the Home Screen first. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `status.remindersNeedReenable` | Reminders need enabling again on this device. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `status.remindersOff` | Reminders are off. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `status.remindersPending` | Reminders are on. Schedule not sent yet. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `status.remindersUnconfigured` | Reminders are not set up on this deployment. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `status.remindersUnsupported` | This browser cannot receive push notifications. | same | same | no: THE REMINDER STATE AND ITS CONTROLS |

### `settings.reminders.lead-times`

Reminder lead times. The day-of time and the lead times before a session.

Renders: `src/ui/components/ReminderSettingsPanel.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.reminderDayOfTime` | Day-of reminder time | same | same | no: THE REMINDER STATE AND ITS CONTROLS |
| `label.reminderLead` | {minutes} minutes before | same | same | yes |
| `label.reminderLeadTimes` | Before each session | same | same | no: THE REMINDER STATE AND ITS CONTROLS |

### `settings.motivation-clip`

Motivation clip. The clip picker, its size limit and the clear control.

Renders: `src/ui/motivation/MotivationSettings.tsx`, `src/ui/settings/DataSection.tsx`

States: `none`, `stored`, `too-large`, `clear-failed`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.clipClearFailed` | The clip was not removed. Everything else was deleted. | same | same | no: RULE 4, a control whose misreading costs data |
| `advice.motivationClipLimit` | The limit is {bytes} bytes. MiB means {mib} bytes. A clip of 25 MiB or less plays back reliably on a phone. | same | same | yes |
| `advice.motivationClipStorage` | Stored on this device, never uploaded. | same | same | yes |
| `button.motivationClipPreview` | Preview | sneak peek | same | yes |
| `button.motivationClipRemove` | Remove clip | same | same | yes |
| `hero.motivationVideo` | Motivation video | same | same | yes |
| `label.motivationClipChoose` | Choose a clip | same | same | yes |
| `label.motivationClipReplace` | Replace the clip | same | same | yes |
| `status.motivationClipNone` | No clip chosen. The bundled clip plays. | same | same | yes |
| `status.motivationClipNotStored` | The clip was not stored. | same | same | yes |
| `status.motivationClipNotVideo` | That file is not a video. | same | same | yes |
| `status.motivationClipStored` | A clip is stored. | same | same | yes |
| `status.motivationClipTooLarge` | That file is over the size limit. | same | same | yes |

### `settings.skin.picker`

Skin picker. Clinical, Limelight and Board, and what a skin may change.

Renders: `src/ui/settings/SkinSettings.tsx`

States: `clinical`, `limelight`, `board`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.skinChanges` | A skin changes wording and colour, never numbers or units. | same | same | no: THE TWO SENTENCES ABOUT THE APP'S OWN RULES |
| `hero.skin` | Look and sound | the look and the noise | same | yes |
| `label.settingsSkin` | Skin | the look | DISPLAY | yes |
| `option.skinBoard` | Board | same | same | no: THE THREE SKIN NAMES |
| `option.skinClinical` | Clinical | same | same | no: THE THREE SKIN NAMES |
| `option.skinLimelight` | Limelight | same | same | no: THE THREE SKIN NAMES |

### `settings.skin.sounds`

Sounds toggle. The switch for the app sounds.

Renders: `src/ui/settings/SkinSettings.tsx`

States: `on`, `off`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.settingsSounds` | Sounds | the noise | same | yes |

### `settings.skin.hotkeys`

Hotkeys toggle. The switch for the single-character shortcuts.

Renders: `src/ui/settings/SkinSettings.tsx`

States: `on`, `off`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.hotkeysOff` | Off leaves Escape and the modifier shortcuts bound. | same | same | no: THE TWO SENTENCES ABOUT THE APP'S OWN RULES |
| `label.settingsHotkeys` | Keyboard shortcuts | the shortcuts | same | yes |

### `settings.data.export`

Export. The JSON backup, the text summary and the calendar file.

Renders: `src/ui/views/ExportView.tsx`, `src/domain/export/summary.ts`

States: `ready`, `no-profile`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.calendarAlarms` | An imported alarm may not fire. | same | same | yes |
| `advice.exportUnavailable` | The data could not be read. Nothing downloaded. | same | same | yes |
| `advice.jsonIsBackup` | The JSON file is the complete backup. | same | same | yes |
| `advice.targetsNotEstimatedForProfile` | Targets are not estimated for this profile. | same | same | yes |
| `button.downloadCalendar` | Download calendar .ics | same | same | yes |
| `button.downloadJson` | Download JSON | same | same | yes |
| `button.downloadSummary` | Download summary .txt | same | same | yes |
| `hero.exportImport` | Export and import | same | same | no: RULE 4, a control whose misreading costs data |
| `label.downloads` | Downloads | same | same | yes |

### `settings.data.import`

Import. The file picker, and what an invalid file does.

Renders: `src/ui/views/ExportView.tsx`

States: `idle`, `invalid`, `done`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.fileUnreadable` | The file could not be read. | same | same | yes |
| `advice.importInvalidNoChange` | An invalid file changes nothing. | same | same | yes |
| `advice.importParseFailed` | That text is not JSON. Nothing changed. | same | same | yes |
| `advice.importReplaces` | Importing replaces everything on this device. | same | same | yes |
| `button.checkImport` | Check import | same | same | yes |
| `button.downloadBackup` | Download backup | same | same | no: RULE 4, a control whose misreading costs data |
| `button.replaceData` | Replace data | same | same | yes |
| `label.chooseExportFile` | Choose an export file | same | same | yes |
| `label.confirmReplace` | Confirm replace | same | same | yes |
| `label.importSection` | Import | same | same | yes |
| `label.pasteExport` | Paste a previous export | same | same | yes |
| `status.importOk` | Imported. The state was replaced. | same | same | yes |

### `settings.data.on-device`

Data on device. The heading and the sentence saying where the document is held.

Renders: `src/ui/settings/DataSection.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.dataOnDevice` | Everything stays on this device. No account. | same | same | no: RULE 4, a control whose misreading costs data |
| `hero.dataOnDevice` | Data on this device | same | same | no: RULE 4, a control whose misreading costs data |

### `settings.data.wipe`

Wipe. The control that destroys the document, and its confirm.

Renders: `src/ui/settings/DataSection.tsx`

States: `idle`, `confirming`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.wipeRemoves` | Removes every profile, plan, session, set and note on this device. | same | same | no: RULE 4, a control whose misreading costs data |
| `button.wipeAll` | Wipe all data | same | same | no: RULE 4, a control whose misreading costs data |
| `button.wipeConfirm` | Delete everything | same | same | no: RULE 4, a control whose misreading costs data |
| `label.confirmWipe` | Confirm wipe | same | same | no: RULE 4, a control whose misreading costs data |

### `settings.data.legacy-delete`

Legacy delete. The old app keys, the download that must precede the delete.

Renders: `src/ui/settings/DataSection.tsx`

States: `absent`, `present`, `confirming`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.deleteLegacy` | Removes the old app data. Anything not imported is lost. | same | same | no: RULE 4, a control whose misreading costs data |
| `advice.legacyOldDataKept` | The old app data was kept. | same | same | no: RULE 4, a control whose misreading costs data |
| `button.deleteLegacy` | Delete legacy data | same | same | no: RULE 4, a control whose misreading costs data |
| `button.downloadLegacyJson` | Download legacy JSON | same | same | no: RULE 4, a control whose misreading costs data |
| `button.legacyDeleteOld` | Delete old data | same | same | no: RULE 4, a control whose misreading costs data |
| `button.legacyReopen` | Import old data | same | same | no: RULE 4, a control whose misreading costs data |
| `label.confirmDeleteLegacy` | Confirm legacy delete | same | same | no: RULE 4, a control whose misreading costs data |

## install (3)

### `install.intro`

Install guide heading. The heading and the sentence naming what only the installed app can do.

Renders: `src/ui/components/InstallGuide.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.installOnlyInstalledApp` | Only an installed app receives notifications. | same | same | no: THE INSTALL GUIDE |
| `hero.installHomeScreen` | Install the app | same | same | no: THE INSTALL GUIDE |

### `install.ios`

Install on iOS. The Safari, Share and Add to Home Screen steps.

Renders: `src/ui/components/InstallGuide.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.installIosVersion` | Requires iOS 18.4 or later. | same | same | no: THE INSTALL GUIDE |
| `label.installIos` | iPhone and iPad | same | same | no: THE INSTALL GUIDE |
| `status.installIosAdd` | Scroll down, tap Add to Home Screen, then tap Add. | same | same | no: THE INSTALL GUIDE |
| `status.installIosOpen` | Open the app from the Home Screen icon. | same | same | no: THE INSTALL GUIDE |
| `status.installIosSafari` | Open this page in Safari. | same | same | no: THE INSTALL GUIDE |
| `status.installIosShare` | Tap Share, the square with an upward arrow. | same | same | no: THE INSTALL GUIDE |

### `install.android`

Install on Android. The menu and Install app steps.

Renders: `src/ui/components/InstallGuide.tsx`

States: `default`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.installAndroid` | Android Chrome | same | same | no: THE INSTALL GUIDE |
| `status.installAndroidInstall` | Tap Install app, then confirm. | same | same | no: THE INSTALL GUIDE |
| `status.installAndroidMenu` | Open the browser menu, the three dots. | same | same | no: THE INSTALL GUIDE |
| `status.installAndroidOpen` | Open the app from the home screen icon. | same | same | no: THE INSTALL GUIDE |

## popup (13)

### `popup.missed-week`

Missed-week popup. The clip, the two sound lines and the single Dismiss control.

Renders: `src/ui/motivation/MotivationModal.tsx`, `src/ui/motivation/MotivationGate.tsx`, `src/domain/motivation/trigger.ts`

States: `muted`, `unmuted`, `no-clip`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.motivationPreview` | Preview. No week is reported. | same | same | yes |
| `advice.tapForSound` | Tap the video for sound. | same | same | yes |
| `advice.tapToMute` | Tap the video to mute. | same | same | yes |
| `advice.weekMissed` | Week of {monday}: {completed} of {target} sessions completed. | same | same | yes |
| `advice.weekMissedNone` | Week of {monday}: no sessions completed. | same | same | yes |
| `hero.motivationPreview` | Video preview | same | same | yes |

### `popup.phase-transition`

Phase transition. The block change cutscene.

Renders: `src/ui/components/PhaseTransition.tsx`

States: `entering`, `shown`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.blockMassMoved` | mass moved | same | same | yes |
| `label.blockSessionsCompleted` | sessions completed | same | same | yes |
| `label.blockSetsLogged` | sets logged | same | same | yes |
| `label.blockSpecimens` | specimens collected | same | same | yes |
| `status.blockTransition` | Block {from} to Block {to} | same | same | yes |

### `popup.spotlight`

Spotlight palette. The command palette, its filter and its results.

Renders: `src/ui/components/Spotlight.tsx`

States: `open`, `no-results`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.noSpotlightMatch` | No matches. | nothing. | same | yes |
| `hero.spotlight` | Search | find it | same | yes |
| `label.spotlightExercise` | Exercise | same | same | yes |
| `label.spotlightQuery` | Search views and exercises | same | same | yes |
| `label.spotlightResults` | Search results | same | same | yes |
| `label.spotlightView` | View | same | same | yes |

### `popup.konami`

Konami overlay. The overlay the sequence raises, and the way out.

Renders: `src/ui/components/KonamiOverlay.tsx`

States: `open`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `status.konami` | No cheat code found. A squat cannot be skipped. | no cheat code. nobody skips a squat. | same | yes |

### `popup.capsule-write`

Capsule write. The note field, the open date and the seal control.

Renders: `src/ui/components/TimeCapsule.tsx`

States: `empty`, `too-short`, `too-long`, `date-out-of-range`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.capsuleDateRange` | Pick a date between {from} and {to}. | same | same | yes |
| `advice.capsuleDefaultMoved` | The suggested date moved into the allowed range. | same | same | yes |
| `advice.capsuleNoteLong` | Longer than {count} characters. | same | same | yes |
| `advice.capsuleNoteShort` | Write at least {count} characters. | same | same | yes |
| `button.sealCapsule` | Seal capsule | seal it | same | yes |
| `label.capsuleNote` | Note to your future self | same | same | yes |
| `label.capsuleOpensOn` | Opens on | same | same | yes |

### `popup.capsule-read`

Capsule read. The note as it reads on the open date.

Renders: `src/ui/components/TimeCapsule.tsx`

States: `sealed`, `openable`, `opened`

No copy key. Review its layout and behaviour only.

### `popup.confirm-destructive`

Destructive confirm. The dialog that gates the wipe and the legacy delete.

Renders: `src/ui/components/ConfirmDestructive.tsx`

States: `download-pending`, `type-to-confirm`, `armed`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.exportBeforeConfirm` | Download the backup before confirming. | same | same | yes |
| `status.exportTaken` | Backup downloaded. | same | same | yes |

### `popup.modal-shell`

Modal shell. The frame every dialog shares: focus trap, close, backdrop.

Renders: `src/ui/components/ModalShell.tsx`

States: `open`

No copy key. Review its layout and behaviour only.

### `popup.migration.explain`

Migration, explain. What the old data is and what the import will do.

Renders: `src/ui/migration/MigrationWizard.tsx`, `src/ui/migration/MigrationGate.tsx`

States: `explain`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.legacyFound` | The old app has data on this device. | same | same | yes |
| `advice.legacyNothingDeleted` | Nothing is deleted until you ask. | same | same | yes |
| `advice.legacyUnitRequired` | Choose a unit. A wrong unit rescales every load. | same | same | yes |
| `button.legacyDismiss` | Start clean | same | same | yes |
| `button.legacyPreview` | Preview import | same | same | yes |
| `disclosure.legacyBodyMassEvidence` | The old body-mass field was labelled lb, refused values outside the pound range, and its text export reported pounds. The load field carried no such evidence, so that unit is asked for. | same | same | yes |
| `disclosure.legacyTransfers` | Logged sets, weekly push-up maxima, body-mass check-ins, beverage intake, daily notes, collected specimen cards, and a sealed time capsule. Anything the import cannot read is listed with its reason first. | same | same | yes |
| `disclosure.whatTransfers` | What transfers | same | same | yes |
| `hero.legacyImport` | Import from the old app | same | same | yes |
| `label.legacyBodyMassUnit` | Body mass unit in the old app | same | same | yes |
| `label.legacyLoadUnit` | Load unit in the old app | same | same | yes |
| `label.legacyMassUnitKg` | Kilograms (kg), body mass | same | same | yes |
| `label.legacyMassUnitLb` | Pounds (lb), body mass | same | same | yes |
| `label.legacyUnitKg` | Kilograms (kg) | same | same | yes |
| `label.legacyUnitLb` | Pounds (lb) | same | same | yes |

### `popup.migration.preview`

Migration, preview. What the import found, before anything is written.

Renders: `src/ui/migration/MigrationWizard.tsx`

States: `preview`, `refused`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.legacyApplyFailed` | The import was refused. Nothing changed. | same | same | yes |
| `advice.legacyRefused` | The old data could not be read. Nothing changed. | same | same | yes |
| `button.legacyApply` | Keep import | same | same | yes |
| `disclosure.whatWasNotMatched` | Days with no session match | same | same | yes |
| `disclosure.whatWasRefused` | What was refused, and why | same | same | yes |
| `hero.legacyPreview` | What the import writes | same | same | yes |

### `popup.migration.applied`

Migration, applied. What was written, and what happens to the old keys.

Renders: `src/ui/migration/MigrationWizard.tsx`

States: `applied`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.legacyOldDataDeleted` | The old app data is deleted. | same | same | yes |
| `advice.legacyStoreUnconfirmed` | The write was not confirmed. The old data stays. | same | same | yes |
| `advice.legacyStored` | The import is stored. The old data is untouched. | same | same | yes |
| `button.legacyClose` | Close | same | same | yes |
| `button.legacyKeepOld` | Keep old data | same | same | yes |
| `hero.legacyDone` | Import complete | same | same | yes |

### `popup.form-cues`

Form cues. The technique reference for one exercise.

Renders: `src/ui/components/FormCuesModal.tsx`

States: `open`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.noFormCues` | No form cues for this exercise. | same | same | yes |
| `label.caution` | Caution | same | same | yes |
| `label.cueExecution` | Execution | same | same | yes |
| `label.cueMistakes` | Common mistakes | same | same | yes |
| `label.cueSetup` | Setup | same | same | yes |
| `label.cueTip` | Tip | same | same | yes |
| `label.formCues` | Form cues and common mistakes | same | same | yes |

### `popup.video`

Video modal. The clip player and its close control.

Renders: `src/ui/components/VideoModal.tsx`

States: `open`, `failed`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `advice.noClipRecorded` | No clip for this exercise. | same | same | yes |
| `button.nextInstance` | Next instance | same | same | yes |
| `button.openClip` | Open clip | same | same | yes |
| `button.searchInstance` | Search instance | same | same | yes |
| `label.formReference` | Form reference | same | same | yes |
| `label.videoSearch` | Search | same | same | yes |
| `status.videoInstance` | instance {shown} of {total} | same | same | yes |

## toast (5)

### `toast.undo`

Undo toast. The toast that offers to undo a delete, with its deadline.

Renders: `src/ui/components/ToastQueue.tsx`

States: `live`, `expired`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `button.undo` | Undo | same | same | yes |

### `toast.milestone`

Milestone toast. The set-count milestone line.

Renders: `src/ui/components/ToastQueue.tsx`

States: `shown`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `status.milestoneSets` | {count} sets recorded. | {count} sets recorded. no crumbs. | same | yes |

### `toast.coach`

Coach toast. The progression line after a logged set.

Renders: `src/ui/components/ToastQueue.tsx`, `src/domain/training/coach.ts`

States: `shown`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `coach.aboveRange` | {count} reps above the prescribed range. | {count} reps above the prescribed range. no crumbs. | same | yes |
| `coach.aboveRangeOne` | {count} rep above the prescribed range. | {count} rep above the prescribed range. no crumbs. | same | yes |
| `coach.belowRange` | {reps} reps, below the prescribed {lo}-{hi}. | {reps} reps, below the prescribed {lo}-{hi}. it happens. | same | yes |
| `coach.durationLogged` | {seconds} s logged. | {seconds} s logged. held it. | same | yes |
| `coach.insideRange` | {load} × {reps}, inside the prescribed {lo}-{hi}. | {load} × {reps}, inside the prescribed {lo}-{hi}. locked in. | same | yes |
| `coach.loadPr` | Load personal record. Previous best {load} × {reps}. | load personal record. mother. previous best {load} × {reps}. | same | yes |
| `coach.overSuggested` | {delta} over the suggested load. | {delta} over the suggested load. showing off. | same | yes |
| `coach.repPr` | Repetition personal record at {load}. Previous best {reps} reps. | repetition personal record at {load}. mother. previous best {reps} reps. | same | yes |
| `coach.setLogged` | Set logged. | set logged. next. | same | yes |
| `coach.setReadout` | {load} × {reps} logged. | {load} × {reps} logged. noted. | same | yes |
| `coach.topOfRange` | Top of range at {load} × {reps}. | top of range at {load} × {reps}. ate. | same | yes |
| `coach.underSuggested` | {delta} under the suggested load. | {delta} under the suggested load. still counts. | same | yes |
| `label.coachNote` | Coach | same | same | yes |

### `toast.telemetry`

Telemetry toast. The line reporting a background action.

Renders: `src/ui/components/ToastQueue.tsx`

States: `shown`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `label.telemetry` | Telemetry | same | same | yes |

### `toast.specimen`

Specimen toast. The card-drawn line that points at Atlas.

Renders: `src/ui/components/ToastQueue.tsx`

States: `shown`

| Key | Clinical | Limelight | Board | Limelight row allowed |
| --- | --- | --- | --- | --- |
| `status.specimenAcquired` | {rarity} specimen acquired | new {rarity} specimen. obsessed. | same | yes |

## not rendered

| Key | Why it has no surface |
| --- | --- |
| `advice.noCardsMatch` | src/ui/views/AtlasView.tsx:28 says the key goes unrendered |
| `hero.weekReview` | src/skins/limelight/Icon.tsx:99 lists it as one of the two keys without a call site; the week-review screen is not built |
| `status.prStamp` | src/skins/limelight/Icon.tsx:99 lists it as one of the two keys without a call site; src/ui/components/WeekStamp.tsx:33 says the stamp now reads status.weekMetStamp |

